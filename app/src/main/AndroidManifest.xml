<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.matejpesl1.tecka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Tecka">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- android:theme="@android:style/Theme.Dialog" -->
        <activity
            android:name=".AppPrefsActivity"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:excludeFromRecents="true"
            android:label="@string/prefs_popup_header" />
        <activity
            android:name=".ValidationCountryActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MessagesActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AddCertificateActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".PersonActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".QrActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".CertificateDetailsActivity"
            android:label="@string/app_name" />
    </application>

</manifest>